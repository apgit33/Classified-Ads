{% extends "base.html.twig" %}
{% block body %}
    
    <main>
        <div class="sticky">

        </div>
        
        <div class="container core">
            {% for ad in list_ads %}
            <div class="row section">
                <div class="col-4">
                    <img src="{{ SERVER_URI }}/{{ ad.a_image_url }}" alt="arolds" class="picture">
                </div>
                <div class="col-6">
                    <p>{{ ad.a_title }}
                    <p>{{ ad.c_name }} </br>Prix : {{ ad.a_price }} €</p>
                    <button type="button" id="button" value="">Plus d'informations</i></button>
                </div>
                <div class="col-2">
                    <a class="modif" uk-icon="file-edit" title="Modifier" href=></a>
                    <svg width="1em" height="1em" viewBox="0 0 16 16" class="bi bi-trash" fill="currentColor" xmlns="http://www.w3.org/2000/svg"></svg>
                </div>
            </div>
            {% endfor %}
        </div>
    </main>
    <footer>

    </footer>
    {% endblock %}
    {% block scriptjs %}
        {{ parent() }}
        
    {% endblock %}
    
    
